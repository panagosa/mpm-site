<svg width="200" height="200" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
  <!-- Outermost circle (dark teal) -->
  <circle cx="100" cy="100" r="95" fill="#0d4e46"/>
  
  <!-- Background circle (reddish-orange) -->
  <circle cx="100" cy="100" r="85" fill="#df2e09"/>
  
  <!-- Middle circle (warm medium orange) -->
  <circle cx="100" cy="100" r="75" fill="#e47413"/>
  
  <!-- Inner circle (face - creamy yellow) -->
  <circle cx="100" cy="100" r="60" fill="#eece91"/>
  
  <!-- Eyes -->
  <circle cx="85" cy="90" r="4" fill="#0d4e46"/>
  <circle cx="115" cy="90" r="4" fill="#0d4e46"/>
  
  <!-- Mouth -->
  <line x1="90" y1="110" x2="110" y2="110" stroke="#0d4e46" stroke-width="2" stroke-linecap="round"/>
</svg>
